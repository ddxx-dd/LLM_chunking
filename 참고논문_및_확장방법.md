# 참고 논문 정리 및 확장 방법 (청킹 비교 프로젝트 관련 연구)

아래 논문들을 참고해서 우리 실험 설계와 보고서의 관련 연구에 활용해줘.
링크는 필요할 때 직접 열어서 확인해줘.

---

# Part 1. 관련 연구

## A. 청킹 방법을 여러 데이터셋에서 비교한 연구

1. **Chunking Methods on RAG – Effectiveness Evaluation Against Computational Cost and Limitations** (2026)
   https://arxiv.org/html/2606.00881v1
   - 청킹 9종(Fixed, Recursive Semantic, TextTiling, LumberChunker 등)을 소설·논문(Qasper)·일반 QA 10개 구성에서 비교
   - 지표: Accuracy@5, Recall@10, LLM-judge, 청킹 시간
   - 결과: Recursive Semantic 89.4% vs Fixed 87.7%로 성능 차이는 작고, 시간은 Fixed <1초 vs LLM 기반 수 시간. 복잡한 방법은 긴 문서에서 타임아웃/메모리 오류가 잦음
   - 활용: 결과표에 "성능 + 청킹 시간 + 실패 여부"를 함께 기록

2. **A Systematic Investigation of Document Chunking Strategies and Embedding Sensitivity** (2026)
   https://arxiv.org/html/2603.06976
   - 36개 전략, 6개 도메인, bge-m3 포함 임베딩 5종
   - 결과: 문단 그룹(구조 보존) 청킹 nDCG@5 약 0.46 vs 고정 글자수 약 0.24
   - 활용: 스마트 청커의 "구조 먼저 자르기" 단계의 근거

3. **The Chunking Paradigm: Recursive Semantic Chunking for RAG Optimization** (ICNLSP 2025)
   https://aclanthology.org/2025.icnlsp-1.15.pdf
   - 의미 기반 그룹화 → 큰 청크 재귀 분할 → 작은 청크 이웃과 병합 (우리 스마트 청커와 가장 비슷)
   - 기준선: RecursiveCharacterTextSplitter, SemanticChunker (우리와 동일)
   - 활용: 가장 가까운 선행연구로 인용. 우리 차별점 = 표·제목·자막 큐 구조 제약 + 토큰 기준 크기 제어

4. **Evaluating Chunking Strategies for RAG on Academic Texts** (2026)
   https://arxiv.org/html/2607.01852v1
   - 학위논문 13편에서 Recursive·Fixed가 클러스터 기반 의미 청킹보다 우수
   - PDF 서식 잔여물(머리글, 번호)이 검색을 오염시켜 일반 질문 점수가 거의 0
   - 활용: PDF 트랙에서 "파싱 품질이 청킹보다 먼저"라는 관찰의 근거

## B. 긴 텍스트 청킹 (LongBench 트랙 관련)

5. **Dynamic Chunking and Selection for Reading Comprehension of Ultra-Long Context in LLMs** (ACL 2025)
   https://arxiv.org/abs/2506.00773
   - 인접 문장 유사도로 경계 탐지 → 512토큰 제한에 맞춰 병합 (우리 알고리즘 2~3단계와 유사)
   - 데이터: Qasper, NarrativeQA, MultiFieldQA, HotpotQA, 2WikiMQA 등 (우리 LongBench 하위셋과 겹침)
   - 결과: 고정 길이 분할보다 크게 우수
   - 활용: LongBench 하위셋 선택과 실험 설정의 근거

## C. 자막·문서 번역 (SRT 트랙 관련)

6. **Translating Movie Subtitles by Large Language Models using Movie-meta Information** (ACL 2025 SRW)
   https://aclanthology.org/2025.acl-srw.22.pdf
   - 이전 자막 2~5줄을 문맥으로 제공, 영화 제목·줄거리 등 메타정보 비교
   - 결과: 이전 4줄 문맥이 도움, 메타정보 중 짧은 영화 제목이 가장 효과적. 긴 줄거리(약 980토큰)는 작은 모델 성능을 오히려 떨어뜨림
   - 지표: BLEU, chrF++, COMET, paired bootstrap 유의성 검정
   - 활용: SRT 평가 지표 세트(COMET 추가), "이전 4큐 문맥" on/off를 세 청커에 동일 적용하는 추가 실험

7. **Doc2FRC: Length-Consistent Document-Level Machine Translation via Fixed-Range Chunking** (2026)
   https://arxiv.org/html/2609.12674
   - 문서를 정해진 토큰 범위(예: 256~512) 안에 들어오게 DP로 분할 → 번역 → 원문 청크와 정렬
   - 결과: 청크 길이 일관성만으로 번역 품질 향상
   - 활용: 스마트 청커의 min/max 토큰 제약 근거, fixed 500자가 한/영에서 토큰 수가 크게 다르다는 문제 지적

## D. 국내 연구

8. **RAG 시스템에서 고정 크기 및 문장 단위 청킹 전략의 평가: KorQuAD1.0 데이터셋 활용 한국어 사례** (KCI)
   https://www.kci.go.kr/kciportal/ci/sereArticleSearch/ciSereArtiView.kci?sereArticleSearchBean.artiId=ART003311446
   - 한국어 청킹 비교 연구 (본문 미확인, 필요하면 직접 확인)
   - 활용: Allganize(한국어) 결과와 함께 인용

---

# Part 2. 확장 방법 (청킹 이후 검색·문맥 단계)

원칙: 프로젝트 주제는 청킹이므로, 아래 기법은 **청커 × 검색기 조합 실험**으로 넣는다.
연구 질문 = "검색기가 좋아져도 스마트 청킹 효과가 남는가?"

## 1. 하이브리드 검색: BM25 + bge-m3 (난이도 하, 우선순위 1)

- 근거
  - Anthropic Contextual Retrieval: https://www.anthropic.com/engineering/contextual-retrieval
    - 검색 실패율(1 − recall@20) 기준: 문맥 임베딩만 35% 감소, 문맥 임베딩 + BM25 49% 감소(5.7% → 2.9%), 여기에 리랭킹까지 67% 감소(→ 1.9%)
    - BM25와 임베딩 결과는 rank fusion으로 결합, top-20이 가장 좋았음
  - From BM25 to Corrective RAG: Benchmarking Retrieval Strategies for Text-and-Table Documents (2026)
    https://arxiv.org/html/2604.01733v1
    - 금융(표+텍스트) 문서에서 BM25가 최신 dense 검색보다 우수
    - 검색 실패의 73%가 표 구조 불일치 때문 → 표가 많은 Allganize와 직접 관련
    - 하이브리드(RRF) + 리랭킹이 최고: Recall@5 0.816, MRR@3 0.605
- 방법
  - 한국어는 Kiwi 형태소 분석기로 토큰화한 BM25 (참고: https://wikidocs.net/251980)
  - 두 결과는 RRF(Reciprocal Rank Fusion)로 합침
  - 대안: bge-m3 자체의 sparse 점수(lexical weights) 사용 → 추가 모델 불필요
    (https://huggingface.co/BAAI/bge-m3, FlagEmbedding의 BGEM3FlagModel)
- 비용: BM25는 CPU만 사용

```python
from langchain_community.retrievers import BM25Retriever
from langchain.retrievers import EnsembleRetriever
from kiwipiepy import Kiwi

kiwi = Kiwi()
kiwi_tok = lambda t: [tok.form for tok in kiwi.tokenize(t)]
bm25 = BM25Retriever.from_documents(chunks, preprocess_func=kiwi_tok, k=20)
dense = build_retriever(chunks, embeddings, k=20)          # 기존 indexing.py
hybrid = EnsembleRetriever(retrievers=[bm25, dense], weights=[0.5, 0.5])  # 내부적으로 RRF
```

## 2. 리랭커: bge-reranker-v2-m3 (난이도 하, 우선순위 2)

- 근거: 위 Anthropic 실험(67% 감소), T2-RAGBench(하이브리드 + 리랭킹 최고)
- 방법: 하이브리드로 20개 검색 → 리랭커로 재정렬 → 상위 5개 사용
- 비용: 약 2GB, bge-m3와 함께 올려도 24GB에 여유 있음

## 3. 청크 문맥 붙이기: 무료 버전 vs LLM 버전 (난이도 하~중, 우선순위 3)

- 근거: Anthropic Contextual Retrieval — LLM이 청크마다 "어느 문서의 어떤 부분인지" 50~100토큰을 생성해 앞에 붙임
- 아이디어: 스마트 청커는 제목·섹션을 이미 알고 있으므로 `문서제목 > 섹션명`을 LLM 없이 공짜로 붙일 수 있음
- 비교 실험: "구조 기반 무료 문맥 vs Gemma 생성 문맥" → 스마트 청커만의 장점을 보여줌
- 주의: Gemma 버전은 청크 수만큼 생성이 필요해 느림 → Allganize 45개 문서 정도에만 적용
- 붙인 문맥은 검색(임베딩·BM25)에만 쓰고, 병합 결과에는 넣지 않는다

## 4. 작게 검색하고 크게 넘기기: small-to-big (난이도 중, 선택)

- 아이디어: 작은 청크로 정확히 검색 → LLM에는 그 청크가 속한 섹션 전체를 전달
- 구현: LangChain `ParentDocumentRetriever`, 부모 = 스마트 청커의 섹션
- 관련 연구
  - Mix-of-Granularity (COLING 2025): 질문마다 최적 청크 크기를 고르는 라우터 학습
    https://arxiv.org/abs/2406.00456
  - HiChunk (ACL 2026): 계층형 청크 + 검색 시 Auto-Merge
    https://arxiv.org/abs/2509.11552
- 우리 범위: 학습 없는 규칙 기반 버전(섹션 = 부모)만

## 5. RAPTOR: 요약 트리 검색 (난이도 상, 시간 남으면)

- RAPTOR (ICLR 2024): https://arxiv.org/abs/2401.18059
  - 청크를 묶어 요약 → 요약을 다시 요약해 트리 생성 → 여러 층에서 검색
  - NarrativeQA처럼 구조 없는 긴 글에서 효과
- 활용: 스마트 청커가 약할 것으로 예상되는 LongBench narrativeqa 보완 + RFP의 "요약"과 연결

## 6. SRT 번역 확장 (난이도 하)

- 근거: 위 논문 6 (ACL 2025 SRW) — 이전 4줄 문맥 + 영화 제목 한 줄이 가장 효과적
- 방법: 두 가지를 세 청커 모두에 on/off로 동일 적용 → 청킹 효과와 문맥 효과 분리
- 이전 큐는 "번역하지 말고 참고만" 하도록 프롬프트에 넣어, 같은 큐가 두 번 번역되지 않게 함 (타임스탬프 병합 유지)

---

# Part 3. 실험 표 구성

| 검색기 \ 청커 | fixed | semantic | smart |
|---|---|---|---|
| dense (bge-m3) | | | |
| BM25 (Kiwi) | | | |
| hybrid (RRF) | | | |
| hybrid + rerank | | | |

- 가로로 읽으면 청킹 효과, 세로로 읽으면 검색기 효과
- 리랭커가 청커 간 차이를 줄이면 → "좋은 검색기가 나쁜 청킹을 어느 정도 메워준다"
- 차이가 유지되면 → "스마트 청킹은 검색기와 독립적으로 효과가 있다"
- 어느 쪽이든 보고서의 결론이 됨

# Part 4. 우리 프로젝트에 가져올 아이디어 요약

1. 결과표에 청킹 시간과 실패 여부를 함께 기록 (논문 1)
2. SRT에 "이전 4큐 문맥 + 영화 제목" on/off 실험을 세 청커 모두에 동일 적용 (논문 6)
3. 관련 연구 절은 논문 3과 5를 기준으로 작성, 차별점(구조 제약 + 토큰 크기 제어 + SRT 타임스탬프 병합) 강조
4. 검색기 확장 우선순위: 하이브리드(BM25+dense) → 리랭커 → 무료 breadcrumb 문맥. small-to-big과 RAPTOR는 중간발표 이후 선택
