# DIP Challenge Lab — 프로젝트 2-1
대용량 비정형 텍스트 스마트 청킹·병합 파이프라인

## 문제 정의
LLM은 한 번에 받는 토큰 수에 한계가 있다. 흩어진 수십 개 .docx나
타임스탬프가 포함된 대용량 자막 파일을 단순 글자 수로 자르면 문맥이 끊긴다.
텍스트를 의미 단위(semantic boundary)로 분할(split)하고, 처리(번역/요약) 후
다시 완벽히 병합(merge)하는 자동화 Python 파이프라인을 만든다.

핵심 질문: **단순 글자수 분할과 의미 기반 분할이 최종 결과
(요약 품질·번역 자연스러움)에 어떤 영향을 미치는가**

## RFP 핵심 수행 업무 4가지
1. 정규식 + python-docx/os/io 로 파일 I/O 시스템 설계   → 전처리(loader)
2. 임베딩 기반 코사인 유사도로 스마트 청킹 알고리즘 개발  → 의미 분할
3. 토크나이저(BPE 등)로 엄격한 토큰 카운팅·로드 밸런싱   → 제약
4. 두 분할 방식이 최종 결과에 미치는 영향 비교          → 평가

## 권장 데이터 셋
- OpenSubtitles - 타임스탬프 처리/병합 테스트용 다용량 다국어 자막
- Longbench / SCROLLS - 긴 문맥처리 평가 벤치마크
- 자체: 연구실 문서 / 강의자료 .docx묶음으로 실제 병합 시나리오 구성

## 관련 자료 / 사이트
- 개념: Langchain Text Splitters 문서 / Pinecone Chunking 가이드
- 토크나이저: Hugging Face Tokenziers / tiktoken
- 파일 처리: python-docx / PYMuPDF

## RFP 최종 산출물 (9절)
- GitHub 리포: 전처리 코드, README(설치·실행·재현 절차). **재현 가능성 필수**
- Tech Report: 배경·관련연구·제안방법·실험설정·결과(정량/정성)·결론과 한계.
  **OOM 등 트러블슈팅 경험 반드시 포함**
- 성과 공유회: 데모 시연 + 발표

우수 결과물의 조건: ① 정직한 비교(동일 조건) ② 명확한 어블레이션
③ 재현 가능한 코드 ④ 한계의 솔직한 서술

## 권장 스택 (RFP 명시, 실제 채택 스택은 아래 "현재 구현 상태" 참고)
- 임베딩: BAAI/bge-m3
- 프레임워크: Sentence-Transformers, LangChain, LlamaIndex
- 평가용 경량 LLM: Qwen3-4B / Gemma-3-4B (24GB에서 로컬 구동)
- 데이터셋: OpenSubtitles, LongBench/SCROLLS, 자체 .docx 묶음
- GPU: Tier 1 (RTX 3090 / A5000). 이 프로젝트는 GPU보다 CPU/메모리/코드 설계가 핵심

---

# 절대 바꾸지 말 것 (알고리즘의 본질)

## fixed_chunking — 단순 글자수 분할
글자 수로만 자른다. 문장·단어·표·자막 큐를 **전혀 고려하지 않는다.**
여기에 문장 경계 존중이나 구조 보호를 덧붙이면 '글자수 분할'이 아니게 된다.

## semantic_chunking — 의미 기반 분할
인접 문장의 코사인 유사도로만 자른다. 구조 정보(타임스탬프·표 행)를 쓰지 않는다.
표를 피해가게 하거나 큐 경계에 스냅시키는 후처리를 붙이지 않는다.
(단, 청크 "크기"를 재는 단위는 토큰으로 바꿨다 — 아래 "청킹 튜닝" 참고. 이건 자르는
기준인 코사인 유사도 자체를 바꾼 게 아니라 크기 제한의 측정 단위만 바꾼 것이라 규칙 위반이 아니다.)

## 구조는 청커의 몫이 아니다

---

# 자료구조 — 청킹이 유닛을 중간에 끊어도 어떻게 복원하는가

## 핵심 모델 (`src/core.py`)

```
Doc(name, text, units, fmt, log)
Unit(start, end, kind, meta)   # start/end는 doc.text 안의 문자 오프셋
Chunk(text, start, end)        # 항상 chunk.text == doc.text[chunk.start:chunk.end]
```

**원칙: 청커는 `doc.text`(순수 문자열)만 본다. 유닛(자막 큐, 표 행)의 경계는 전혀 모른다.**
구조 정보(타임스탬프, 표 행 번호 등)는 전부 `Unit.meta`에 텍스트와 분리되어 있고,
청킹이 끝난 뒤 병합 단계에서만 문자 오프셋 겹침으로 다시 매칭한다.

## SRT(자막) 로드 — `preprocessing/loader.py: load_srt`

자막 큐 하나 = `Unit` 하나. `kind="cue"`, `meta={"index":큐번호, "t_start":초, "t_end":초}`.
`doc.text`는 모든 큐 텍스트를 이어붙인 하나의 문자열(유닛 경계 정보 없음).

## 청킹 → 번역 → 병합 흐름 (`pipeline/mapper.py`)

1. **`fragments(doc, chunk)`** — 청크 하나가 어떤 유닛(들)과 겹치는지 문자 오프셋 교집합으로 찾는다.
   반환: `(유닛인덱스 j, 겹치는 구간 시작 s, 끝 e, 유닛 전체 포함 여부 w)`.
   유닛 하나가 청크 경계에 걸리면 **두 개의 청크에서 각각 다른 fragment로 잡힌다.**
2. **`build_prompt(doc, chunk)`** — 그 청크 안의 fragment들을 `[1] ...`, `[2] ...`처럼
   번호 태그를 붙여 LLM에 보낸다. 유닛이 잘렸다면 그 조각(예: 문장 앞부분만)이 번호 하나를 차지한다.
3. **`parse_marked(llm_output, frs)`** — LLM 응답에서 번호별 번역 결과를 다시 뽑아
   `(j, 번역텍스트)` 목록으로 되돌린다.
4. **`merge_to_units(doc, all_translated_pieces)`** — **여기가 핵심.**
   유닛 인덱스 `j`별로 버킷을 만들고, 모든 청크에서 나온 `(j, 텍스트)` 조각을 해당 버킷에 쌓은 뒤
   순서대로 공백으로 이어붙인다. 유닛이 청크 경계에 안 걸렸으면 버킷에 조각 1개,
   걸렸으면 조각 2개 이상이 들어와 자동으로 이어붙여진다.
5. **`write_srt(doc, unit_texts, out_path)`** — 병합된 텍스트를 **원본 유닛의 타임스탬프**(`u.meta["t_start"/"t_end"]`)
   그대로 써서 SRT를 생성한다. 번역이 아무리 잘려도 타임스탬프는 원본과 100% 동일하게 보존된다
   (`eval/subtitle_translate.py: check_timestamp_integrity`가 이걸 검증).

### 예시 (가상 대사, 실제 대본 아님)

원본 자막 큐 하나: `"안녕, 오늘 좀 피곤해 보이네"` (유닛 #5, 0:10~0:14)

고정 글자수 분할이 이 유닛 중간을 잘라 두 청크에 걸치면:
```
청크 A (앞부분): ... "안녕, 오늘 좀"          -> 태그 [3]
청크 B (뒷부분): "피곤해 보이네" ...          -> 태그 [1]
```
각 청크는 **독립적으로** LLM에 번역 요청되고(청크 B는 청크 A의 문맥을 전혀 모름),
각각 `(5, "Hi, today...")`, `(5, "you look tired")` 같은 결과를 낸다.
`merge_to_units`가 유닛 #5 버킷에 이 둘을 순서대로 이어붙여
`"Hi, today... you look tired"`가 되고, 이 텍스트가 **원본 유닛 #5의 타임스탬프(0:10~0:14)** 그대로
최종 SRT에 기록된다. 문맥 없이 잘려서 번역이 어색해질 수 있는데, 이게 바로 고정 글자수 분할의
한계를 그대로 드러내는 지점이다 — 병합이 실패한 게 아니라 의도대로 정직하게 드러난 것.

## DOCX(표) 로드 — `preprocessing/loader.py: load_docx`

표 행 하나 = `Unit` 하나. `kind="row"`, `meta={"table":표번호, "row":행번호, "header":헤더목록}`,
텍스트는 그 행을 JSON 한 줄로 직렬화한 것. 청킹이 표의 행들을 서로 다른 청크로 쪼개거나
top-k 검색에서 일부 행만 뽑혀도, 그 결손을 보정하지 않고 그대로 드러낸다
(`eval/docx_compose.py: extract_row_dicts`가 검색된 청크에 실제로 들어있는 행만 원본 그대로 표로 렌더링).

---

# 현재 구현 상태 (2026-09-12 기준)

RFP가 제안한 스택(Qwen3-4B/Gemma-3-4B)에서 출발했지만, 실증 비교 끝에 아래처럼 바뀌었다.
"권장 스택"은 역사적 참고용으로 위에 그대로 남겨두고, 실제로 쓰는 건 이 섹션이 최신이다.

## LLM 선택 (실증 비교 결과)
순서대로 비교: Qwen3-4B(고유명사 오역 편향, 예: "Truman"→"트럼") → Qwen3-8B(동일 편향, 기각)
→ Llama-3.1-8B-Instruct(고유명사 오류 0건, 가장 빠름, 단 라이선스 제약) → Gemma-4-E4B-it(Apache 2.0,
양자화 불필요) → Gemma-4-12B-it 8bit(bitsandbytes 이상치분해로 지나치게 느림, 폐기)/4bit(빠르고
품질 좋음) → Gemma-4-26B-A4B(MoE, "활성 파라미터"는 연산량 지표일 뿐 메모리 지표가 아님 — 24GB에서
4bit로도 실제 OOM 확인, 완전 폐기) → **최종: `google/gemma-4-12B-it-qat-q4_0-unquantized`(QAT, 4bit)**.
QAT(양자화 인식 학습) 체크포인트라 일반 사후 4bit보다 chrF/BLEU/F1이 전반적으로 더 나음이 실측 확인됨.
캐시엔 이 QAT 모델과 `BAAI/bge-m3`만 남아있다(나머지 전부 삭제됨).

## 아키텍처
- `config.py`의 `TOKENIZER`가 전체 파이프라인이 쓰는 모델의 단일 소스.
- `llm/client.py: load_llm()`은 `model_id`에 "gemma-4"가 포함되면
  `Gemma4UnifiedForConditionalGeneration` + 4bit `BitsAndBytesConfig`(NF4)로 로드.
  `device_map={"": 0}` 고정 사용 — `"auto"`는 4bit 모델 일부를 CPU로 오프로드하려다
  bitsandbytes가 거부하는 에러를 냄.
- `generate()`는 단건 생성, `generate_batch()`는 여러 messages를 한 번에 배치 생성
  (왼쪽 패딩 표준 사용). 자막 번역 파이프라인(`eval/subtitle_translate.py`)은
  `translate_chunks_batch()`로 기본 **batch_size=8**을 씀 — batch_size별 속도 실측(16청크
  기준, batch=1 대비 배수): 1(365.6초, 1.00배) → 2(211.6초, 1.73배) → 4(131.2초, 2.79배) →
  **8(84.8초, 4.31배)**. 메모리는 batch=1(10.21GB)→8(12.59GB)로 완만하게만 증가(24GB 중
  절반 수준이라 여유 있음). 배치 처리는 순차 처리와 결과 텍스트가 100% 동일하진 않음(13%
  정도 줄바뀜) — GPU 배치 행렬연산의 부동소수점 비결합성 때문이며, 내용이 깨지는 게 아니라
  자연스러운 패러프레이즈 수준 차이임 (버그 아님, 알려진 현상).
- `eval/glossary.py`, `eval/script_check.py`는 삭제됨 — Gemma 계열은 Qwen3-4B가 필요로 했던
  고유명사 용어집/스크립트 검증 재시도 장치가 필요 없음.

## 평가 지표
- 번역: word-F1 대신 **chrF + BLEU**(둘 다 `sacrebleu`) — word-F1이 한국어에 너무 가혹함.
  `chrF2>60`이 문헌상 "좋은 시스템" 기준인데, 이 프로젝트 점수(en2ko ~14-21, ko2en ~33-38)는
  그보다 낮음 — 파인튜닝 없는 프롬프트 기반 EN-KO 번역의 문헌 패턴과 일치, 이상 아님.
- QA(LongBench): word-level F1(SQuAD 스타일 normalize).
- LLM-as-judge는 도입하지 않기로 결정 — non-determinism과 self-preference bias 문헌
  근거(arXiv:2410.21819, 원문 확인) + fairness 원칙과 충돌 우려 때문에 보류.

## 청킹 튜닝
- `semantic_chunking`은 글자 수 대신 **토큰 수**로 크기를 잰다(임베딩 모델 자체의
  `.tokenizer` 사용) — EN/KO 언어별 글자당 토큰 밀도가 달라서 글자수 기준이 부정확했음.
  `min_chunk_tokens`(너무 작은 파편 청크 방지) / `max_chunk_tokens`(무한정 커지는 것 방지)
  두 파라미터 추가. `fixed_chunking`은 규칙대로 글자수 그대로 유지.
- `min_chunk_tokens` 값 64/128/256/512를 실측 비교(About Time, 15청크 미니테스트) —
  512가 chrF/BLEU 최고였으나 표본이 작아 노이즈 가능성 있음. **현재 기본값: 128**
  (`run_docx_compose.py`/`run_longbench.py`/`run_subtitle_translation.py` 공통).
- threshold(`method`/`amount`)와 `min_chunk_tokens`를 합동으로 튜닝하는 실험은 아직 안 함(보류 중).

## 데이터 코퍼스 구성 (자체 수집, docx/pdf)

RFP의 "자체: 연구실 문서 / 강의자료 .docx묶음" 요구에 맞춰 여러 소스를 직접 수집·검증했다.
장르마다 실제로 유통되는 원본 포맷이 다르다는 걸 확인해서(아래 참고) 포맷을 장르별로
다르게 잡았다 - **논문은 PDF, 그 외(설명문/문학/업무문서)는 docx**.

| 장르 | 소스 | 포맷 | 건수 | 라이선스 |
|---|---|---|---|---|
| 논문(한국어) | 의사학(medhist.or.kr, 의학사) | PDF | 5 | CC BY-NC |
| 논문(영어) | PLOS(교육학/환경과학/인지사회심리학 3분야) | PDF | 9 | CC BY |
| 대용량 단일 문서(한국어) | 인간문제(문학, 위키문헌) + 민법 전문(법령) + 대한민국(설명문, 위키백과) | docx | 3 | CC BY-SA/PD(문학·설명문) + 법령(저작권 비대상, 민법) |
| 업무문서 | `RAG-Multi-Corpus`(GitHub, 5개 가상기업 정책/매뉴얼/보고서) | docx | 236 | MIT |
| 행정문서(영어) | 캐나다 정부 오픈데이터(open.canada.ca) 실제 정부기관 문서 | docx | 360 | Open Government Licence - Canada(연방/각 주 변형) |

**논문=PDF, 나머지=docx로 나눈 이유**: 논문을 HTML→docx로 변환해서 쓰다가(PMC/체육과학연구
등) colspan/rowspan 병합·표 위치 누락 버그를 반복해서 겪었는데, 근본적으로 **논문은
docx로 배포되는 문서가 아니다** - PDF(또는 출판사 JATS-XML)가 네이티브 배포 포맷이고,
GROBID/science-parse 같은 논문 전용 파서도 PDF를 직접 입력받는다. docx는 오히려
"저작 중" 포맷이지 출판물의 배포 포맷이 아니다. 그래서 논문 14편은 표 0~2개·병합
최소로 미리 검증한 것만 원본 PDF 그대로 받아서 저장했고, `loader.py`의 `load_pdf()`로
파이프라인에 붙였다(아래 참고). PMC는 NCBI의 cloudpmc-viewer가 JS 기반
proof-of-work 챌린지로 PDF 직접 다운로드를 막아놔서(우회하지 않기로 결정) 코퍼스에서
완전히 제외했다. 아동학회지(4066)·PLOS(pone.0356173)도 제외했다 - `load_pdf()`
설계 중 실제 레이아웃을 확인해보니 이 둘만 2단(two-column) 편집이었고, PyMuPDF
`get_text("blocks")`의 순서 재구성 로직이 2단에서는 왼쪽/오른쪽 단을 섞어버리는
부작용이 있어서, 컬럼 인식 로직을 추가하는 대신(과설계 방지) `load_pdf()`를 "1단만
지원"으로 단순하게 유지하고 2단인 두 편을 코퍼스에서 뺐다. 분야 다양성을 위해
아동학회지(발달심리학, CC BY-NC 3.0)에서 다른 5편을 다시 골랐다가(47권 3호) **전부
2단으로 재확인되어 또 제외** - childstudies.org 저널 자체가 최근 권호는 2단 조판을
쓰는 것으로 보임(최대 블록폭/페이지폭 비율 0.41~0.42로 실측 확인, 1단이면 보통
0.7~0.9대). 다른 분야의 1단 한국어 논문 저널은 아직 못 찾음(미해결).

`kjmh-31-1-35`도 제외했다 - `load_pdf()` 로직을 실제 PDF에 검증해보다가 표가 11개나
잡혀서(원래 "표 0개"로 확인해서 고른 논문) 확인해보니, 이 논문의 부록 표(고서 출처
비교표)가 정적 HTML엔 전혀 없고 JS로만 동적 로딩되는 콘텐츠였다(예전 HTML 검증이
틀린 게 아니라 애초에 그 방법으론 접근 불가능한 정보였음 - PDF는 완성 렌더링이라
다 들어있음). 결과적으로 이 논문은 "표 0~2개" 기준을 원래부터 만족 못 한 것으로
확인돼 제외했다.

`pone.0334738`(Dynamical systems model of emotional contagion)도 제외했다 - 수식이
많아서 LaTeX 조판 수식의 개별 기호(첨자·괄호·연산자)가 PDF 안에서 각각 따로 위치해
있어, 줄 단위 추출이 수식을 기호별로 잘게 쪼개 노이즈성 문단을 대량 생성했다(실측:
짧은 줄 698개). 서술 문장 자체는 안 깨지지만 품질 저하가 커서 제외 - 문헌(PDFBoT 등)
에도 "디스플레이 모드 수식"이 룰베이스 추출의 공통 약점으로 명시된 케이스임.

남은 14편은 전부 1단·표 0~2개·수식 노이즈 없음을 PDF 원본에서 직접 재확인함
(`page.get_text("blocks")`의 블록 x0 폭 비율로 단 여부 판별, `find_tables()`로 표
개수/내용 확인).

**`load_pdf()` 구현 완료 (`loader.py`)** - PyMuPDF(`fitz`) 기반. Docling(레이아웃 인식
모델 기반 문서 변환 라이브러리)도 대안으로 직접 설치해서 비교 테스트했으나, 문단을
통째로 묶어주는 장점은 있지만 표 오탐(0x0 가짜 표, 수식을 표로 오인식)이라는 새
문제가 생기고 무거운 의존성(torch 등)에 논문당 ~20초씩 걸려서, PyMuPDF만 쓰는 쪽으로
확정했다. 핵심 설계(전부 14편 실측 검증 - 표 구조/폰트크기 분리/문장 경계 26곳 직접
읽고 확인, 이슈 0건):
- **반복 헤더/푸터 제거**: 줄(line) 단위로 숫자→`#` 치환 후 30% 이상 페이지에 나오면
  제거(블록 단위로 계산하면 반복 헤더가 블록/줄 불일치로 새는 버그가 있었음).
- **각주/참고문헌 판별**: 문서 전체에서 글자 수 기준 최빈 폰트크기를 본문 크기로
  잡고, 그 90% 미만인 줄을 각주/참고문헌급으로 봐서 본문 흐름에서 빼고 문서 맨
  끝에 원래 순서대로 모아 붙인다(둘을 구분하지 않음 - 둘 다 작은 폰트를 쓰는 경우가
  많아 같은 신호로 잡히고, 구분할 필요도 없다고 확인함). 한 줄의 대표 폰트크기는
  **글자 수 가중평균**으로 계산해야 한다 - 단순평균(span 개수 기준)을 쓰면 본문 줄
  중간에 낀 위첨자 각주번호(짧은 조각) 하나가 평균을 끌어내려 40자짜리 정상 본문
  줄 전체가 각주로 오분류되는 버그가 있었음(`kjmh-34-2-209`에서 실제 발견). 각주를
  즉시 그 자리에 끼워넣지 않고 문서 끝에 모아 붙이는 이유: 페이지 경계에서 문장이
  끊긴 채로 그 사이에 각주가 있으면 본문이 더 이상해지므로, 처음부터 본문 스트림에서
  완전히 빼는 게 가장 단순하고 안전함(실측: 각주 1~25개가 스킵되는 지점 26곳을 직접
  읽어서 전부 단어/문장이 정확히 재조합됨을 확인).
- **페이지 경계에 걸친 표 병합**: 표가 페이지 경계에서 잘리면 `find_tables()`가
  이걸 별도의 두 `Table` 객체로 인식하는데, 이어지는 페이지 쪽 표엔 헤더 행이 없어서
  그 페이지의 첫 데이터 행을 헤더로 잘못 인식하는 문제가 있었다(`kjmh-31-2-181`/
  `kjmh-34-2-209`에서 실제 발견). "바로 다음 페이지 + 그 페이지의 첫 표 + 컬럼 수
  동일"이면 같은 표의 연속으로 보고 행을 그대로 이어붙인다(헤더 재추출 없이 전부
  데이터 행으로 취급) - 위치(bbox) 기반 판정("표 하단이 페이지 하단 여백 근처")은
  실측해보니 안 맞는 경우가 있어서 버리고 이 조건만 씀.
- **문단/각주는 줄(line) 단위로 즉시 Unit을 만들지 않고 연속 버퍼로 병합한다**:
  PDF의 "줄"은 페이지 폭 때문에 생기는 렌더링 산물일 뿐 문장/문단 경계가 아닌데,
  처음엔 줄마다 Unit을 만들고 `\n`으로 이었다가 `kjmh-31-2-181`에서 실제 버그를
  발견함 - "...연구 초점들이 놓여 있는 것은 놀라운 일이" + "아니다. 일례로..."가
  한 문장인데 줄이 갈려서 `split_sentences()`가 이 `\n`을 문장 경계로 오인해
  semantic_chunking이 문장 중간을 잘라버림. 같은 kind(문단 또는 각주)의 줄이
  이어지는 동안 버퍼에 누적하고, kind가 바뀌는 시점(문단↔각주, 표 등장)에만
  하나의 Unit으로 확정하도록 바꿨다. 버퍼는 페이지 루프를 넘어 유지되므로 페이지
  경계에 걸친 문단/각주도 표의 페이지 병합과 같은 이치로 자동 처리됨.
  - **이어붙일 때 구분자를 넣지 않고 원본을 그대로 잇는다**: PyMuPDF가 뽑은 줄
    끝의 공백 유무가 이미 정확한 신호다 - 어절 경계면 공백이 있고("...놀라운 일이 "),
    음절 중간 절단이면 공백이 없다("...과거의 유행" + "병이나..." → 원래 한 단어
    "유행병이나"). 한국어는 줄바꿈 시 하이픈 없이 음절 중간을 그냥 끊는 게 정상
    조판 관행(Unicode UAX #14 확인)이라, 임의로 공백을 넣거나 빼려 하면 오히려
    틀린다 - Docling으로 같은 지점을 테스트했을 때 "유행 병이나"처럼 없던 공백을
    끼워 넣는 오류를 실제로 확인함(레이아웃 모델도 이 모호성을 못 풂).
  - **안전장치는 딱 하나**: 버퍼 끝이 문장부호(`.!?`)인데 공백이 없으면 공백 하나를
    보정해서 붙인다(`_needs_join_space`) - `split_sentences()`의
    `(?<=[.!?])\s+` 정규식이 공백 자체를 분할 지점으로 찾기 때문에, 공백이 없으면
    문장 경계 판별이 애초에 불가능해서 이 경우만 예외적으로 개입해야 함. 정규식
    자체(`\s+` 요구)를 없애는 대신 이렇게 국소적으로 고친 이유: 요구를 없애면
    모든 소수점("1.8%")까지 분할 대상이 되어 버려서(공백 요구가 있어서 지금까지
    "우연히" 보호받고 있었음) 이 함수는 SRT/docx까지 공유하는 공용 코드라 영향
    범위가 전체로 퍼짐 - PDF 전용 병합 로직 안에서만 좁게 고치는 게 안전함.
    각주번호(`"1)"`)가 문장부호 바로 뒤에 공백 없이 붙는 게 한국 학술지의 표준
    표기라(`kjmh-27-1-49` 등에서 실제 발견), 검사 전에 끝의 `\d+\)` 패턴은 떼고
    본다. 투명 유니코드 서식문자(ZWNJ `‌` 등, `pone.0357589`에서 실제
    발견 - 공백처럼 보이지만 `str.isspace()`가 `False`라 공백 취급이 안 됨)는
    `normalize()`의 치환 대상에 함께 추가해서 애초에 진짜 공백으로 바뀌게 함.
  - `normalize()`가 아니라 `_clean_pdf_line()`을 쓴다 - `normalize()`의 마지막
    `strip()`이 줄 끝 공백까지 지워버려서 위 병합 신호가 사라지는 버그가 있었음
    (실측 발견, 병합 전엔 좌우 공백 보존하고 Unit 확정 시점에만 `strip()`).
- heading 메타는 항상 `False`(docx의 Heading 스타일 같은 명시적 구조가 PDF엔 없음 -
  과설계 방지), 2단 레이아웃은 미지원(해당 논문은 이미 코퍼스에서 제외).
- 전체 14편 + docx 413개(합계 427개) `load_file()` 일괄 재로드로 크래시/빈문서
  0건 확인. 안전장치 적용 후 "구두점 뒤 공백없음" 패턴이 일부 남지만 전부 소수점/
  URL/이메일/약어(`e.g.,`, `i.e.,`, `et al.,`) 등 애초에 이 병합 로직과 무관하게
  `split_sentences()`의 `[.!?]` 정규식 자체가 못 구분하는 기존 한계로 확인됨.

**"진짜 .docx 네이티브 한국어 문서"를 찾다가 실패한 경로들** (재탐색 방지 기록 - 전부
직접 fetch로 확인함): 공공데이터포털·PRISM(React SPA라 정적 스크래핑 불가, robots
noindex/nofollow 명시), KOCW(다운로드 파라미터 안 걸림 + 라이선스가 범례인지 실제
적용인지 불분명), 공유마당(URL 구조 변경으로 접근 불가), `superdoc-dev/docx-corpus`
(HF, 73.7만 문서 - 개별 문서 저작권이 불분명한 스크랩 코퍼스 + 한국어 서브셋 58%가
얇은 양식류 + 표가 문서당 29~42개로 과다), 미국 EPA 정책문서 747개 첨부파일 전수조사
(전부 PDF, .docx 0개), USDA(유일한 .docx가 빈 서식 템플릿), korea.kr 정책브리핑
25건 전수조사(첨부파일 전부 hwp+pdf, .docx 0개), GitHub `kordoc`(한국어 문서 파서
프로젝트, 테스트 픽스처에 docx 0개 - hwp/hwpx/xls만 있음). **결론: 완성된 실제
문서는 한국이든 해외든 거의 항상 PDF(한국은 +HWP)로 배포되고, .docx가 원본으로
존재하는 오픈라이선스 코퍼스는 실질적으로 존재하지 않는다(한국어는 이후 추가
조사에서도 계속 유효함 - 아래 참고).** `RAG-Multi-Corpus`는 저자가 직접 합성
생성한 콘텐츠라 이 문제(개별 문서 저작권 불분명)에서 자유롭고, 전수 확인 결과
320개 표 중 colspan/rowspan 병합이 0건이라 채택했다.

**추가 대규모 탐색(Kaggle/GitHub/HuggingFace/학술논문 각 2라운드 + OER/타국정부
오픈데이터/AI에이전트벤치마크/비영리단체·법률템플릿 아카이브)에서 영어 네이티브
docx의 진짜 예외를 발견함 - 정부 오픈데이터 포털**. 미국(.gov, 대체로 퍼블릭도메인),
영국(gov.uk, OGL v3.0), 호주(gov.au, 기본 CC-BY 4.0)는 실제로 소량(Microsoft
`OfficeComprehensionBenchmark` 경유로 확인, 총 9개) 존재했고, **캐나다 정부
오픈데이터 포털(open.canada.ca)은 대규모로 확인됨**:
- CKAN API(`/data/api/3/action/package_search?fq=res_format:DOCX`)로 전수 조회 -
  실제 DOCX 리소스 1,352개(487개 데이터셋), 라이선스는 전부 Open Government
  Licence - Canada 계열(연방 `ca-ogl-lgo`, 온타리오 `on-oglo`, 유콘 `yk-oglyk`,
  퀘벡은 `qc-cc-by`로 명시 등) - CC-BY와 동등하게 재배포·개작 허용.
- 전수 다운로드(1,205개 유효 docx 확보) 후 3단계 필터로 383개 채택
  (`src/scripts/fetch_canada_gov_docx_corpus.py`): ①언어=영어(캐나다는 공용어
  이중고시 의무라 프랑스어 버전도 항상 같이 존재, 영어 635/프랑스어 531로 거의
  균등) ②표 0~1개(그 이상은 대부분 통계·재무보고서류라 급격히 늘어남, 실측
  확인 - 10개/3개/1개로 상한을 낮춰봐도 영어 문서 수는 45~50개/132개로 거의
  안 바뀌어서 표 개수 자체보다 다음 항목이 진짜 관문이었음) **③병합 셀
  (colspan/rowspan) 없음** - `python-docx`가 병합 셀 텍스트를 병합범위 안의
  모든 물리적 칸에 그대로 복제해서 반환하는데, `load_docx()`의 header-value
  zip 로직이 이걸 못 걸러내서 전혀 다른 행의 값이 열 위치만 우연히 맞아
  잘못 짝지어지는 버그를 실제로 발견함(신청서/양식류 문서, 전체의 14~16%에서
  발생) - `load_docx()`를 고치는 대신(다른 정상 소스들에 영향 줄 수 있어서)
  수집 단계에서 배제하는 쪽을 택함(RAG-Multi-Corpus 선정 때와 같은 원칙).
  **엄격 재검증(전체 텍스트 기준 언어 재감지 + 표행 JSON 유효성 + 문단수
  원본대비 손실 여부)에서 23개가 추가로 걸러짐** - 언어 필터가 문서 앞
  500자만 보고 판정해서, 제목/목차만 영어고 본문 대부분이 프랑스어인
  이중언어 문서(파일명에 "fra-eng"/"fr_en" 흔적 있음, 캐나다 공용어 이중고시
  관행상 흔함)가 통과했던 것 - 전체 텍스트로 재감지해서 제외, **최종 360개**.
  (표행 값이 전부 동일해 보이는 케이스 1건도 같이 잡혔으나 확인해보니 "Secret"
  이 영어/프랑스어 표기가 같은 보안등급 대조표라 정상 데이터였음 - 오탐.)
  수집 단계에서 배제하는 쪽을 택함(RAG-Multi-Corpus 선정 때와 같은 원칙).
  Aventro/RAG-Multi-Corpus류의 "합성이라 애초에 안전한" 소스와는 다르게, 이건
  "실제 정부 문서 중 파싱이 안전한 것만 고른" 첫 사례.
- **다른 경로들은 전부 기각/막힘으로 확정**: GovDocs1(2009년 코퍼스라 신형 docx
  자체가 거의 없음, 실측 확인), AI 에이전트 벤치마크 전체(OSWorld/WorkArena/
  TheAgentCompany/MacAgentBench/GAIA - 파일이 git이 아니라 VM/도커 이미지에
  있어 구조적으로 안 맞음, GAIA는 재배포 금지 조항도 있음), Workspace-Bench
  (MIT라 라이선스는 깨끗하나 "영어" 폴더에도 중국어 콘텐츠가 섞여있어 보류),
  OfficeBench(Apache 2.0이지만 중복제거하면 6개뿐), Common Paper 등 법률/
  비영리 아카이브(라이선스는 좋으나 원본이 Markdown이거나 PDF뿐), OER Commons
  (실재하나 대량수집 경로 없고 개인 과제물·빈 파일 혼재).

**`trec_covid` 제거, `docx_kor`을 "장르 다양성"에서 "대용량 단일 문서"로 전면 재편**:
`trec_covid`(BEIR 학술초록 64건)는 원래 QA/IR 벤치마크용으로 넣었으나 실제 질문-답변쌍이
없어서(QA는 `data/longbench/`가 담당) 그냥 짧은 학술초록 산문일 뿐이었고 PLOS가 이미
같은 장르를 담당하고 있어 제거했다. `docx_kor`의 기존 두 장르(Wikipedia_설명문 5개,
Wikisource_문학 16개, 총 21개)도 제거했다 - "여러 개의 짧은/중간 문서로 장르 다양성을
보여주기"엔 영어 쪽(768개)과 비교하면 애초에 규모가 너무 작아 의미 있는 비교가 안 됐고,
대신 "대용량 문서 하나에서 청킹이 어떻게 동작하는가"라는, 기존 어떤 한국어 자료도
못 채워주던 축으로 방향을 바꿨다. 최종적으로 `대용량문서/`에 3개(직접 선정):
- **인간문제**(강경애, 위키문헌 CC BY-SA) - 23.7만자, 장(챕터) 구분이 아예 없는 단일
  페이지라 발췌할 필요 없이 그대로 씀(원래 `Wikisource_문학` 구축 때 "발췌 기준을
  잡기 애매하다"는 이유로 일부러 뺐던 작품인데, 대용량 단일문서 목적에선 오히려
  장점임).

`build_novels_docx.py`(탁류/무정, 장별 하위페이지를 MediaWiki API `allpages`로 목록
조회 후 순서대로 병합)도 만들어서 실제로 검증까지 마쳤으나(탁류 41.1만자/무정
32.3만자, 둘 다 `load_docx()` 정상 통과 확인) 최종 코퍼스에선 빠졌다 - 인간문제 하나로
충분하다고 판단. 다른 장편소설 후보(상록수 25.4만자/태평천하 16.9만자/무영탑 33.7만자/
흙 44.7만자, 전부 저작 사후 70년 경과로 PD)도 같은 스크립트로 언제든 추가 가능. 이
과정에서 `wikitext_lib.py`의 버그도 하나 고침 - `__NOTOC__` 같은 MediaWiki
매직워드가 렌더링 안 되고 문단에 그대로 남는 걸 탁류에서 발견해서 `_clean_inline()`에
제거 로직 추가(이 수정 자체는 유지 - 인간문제/대한민국 등 다른 문서에도 영향 가능).
- **민법 전문**(국가법령정보센터 law.go.kr) - 제1조~제1118조, 총 1,338개 문단(편/장/절
  제목 145 + 조문 1,193), 10.5만자. **법령은 저작권법 제7조에 의해 애초에 저작권 보호
  대상이 아니라서 라이선스 걱정이 전혀 없다** - 지금까지 찾은 어떤 소스보다 깨끗함.
  조회 페이지(`/법령/<이름>`)는 실제 조문이 없는 iframe 껍데기고, 진짜 조문은 그
  iframe이 부르는 `LSW/lsInfoR.do?lsiSeq=...&efYd=...&chrClsCd=010202` 쪽에
  있음(실측 발견) - `편/장/절` 제목은 `<p class="gtit">`, 조문 하나하나는
  `<p class="pty1_p4">`로 깔끔히 구조화돼 있어 그대로 파싱(`build_law_docx.py`).
- **대한민국**(위키백과, CC BY-SA) - 5.06만자, 286문단·표1개(19행). 위키백과 개별
  문서는 구조상 대체로 5만자가 상한이라(직접 확인: 대한민국 5.1만/제2차 세계 대전
  4.0만/태양계 2.6만 등, 다른 문서는 다 이보다 작음) 다른 대용량 문서들보다는
  작지만, 기존 `Wikipedia_설명문`(개별 문서 평균 1.6만자) 대비로는 3배 큰 "설명문"
  장르 대표로 채택.

**한국 정부 산하 연구기관의 "AI 브리프"류 정기 정책보고서(PDF)는 재탐색 불필요** -
SPRi(소프트웨어정책연구소)·KDI(한국개발연구원)·KISDI(정보통신정책연구원) 3곳을
직접 확인한 결과 전부 "공공누리 출처표시-변경금지"(SPRi/KDI) 또는 "제4유형:
출처표시+상업적이용금지+변경금지"(KISDI)라 재가공이 막혀있음 - 한국 공공기관
정책브리프의 흔한 관행으로 보임. NIA/KISTEP/한국은행은 사이트가 JS 렌더링이라
직접 확인은 못 했으나(WebSearch 예산 소진 상태에서 URL 추측만으로는 접근 실패),
같은 패턴일 가능성이 높음.

**법령과 같은 이유로 저작권 비대상인 판례(재판 판결·결정)도 대용량 PDF 후보로
검토했으나** - law.go.kr의 판례 조회 페이지는 검색 결과 목록 자체가 AJAX 뒤에
숨어있어(민법 때처럼 단순히 iframe 하나만 까면 되는 구조가 아님) 이번엔 실제
문서 ID를 못 찾았음 - 재시도 가치는 있음(아직 기각 아니고 미완료).

**`docx_eng`에서도 Gutenberg(문학, 61개)·Wikibooks_WorldHistory(설명문/교재, 31개) 제거** -
RAG-Multi-Corpus(236)·Canada_Government(360)라는 훨씬 크고 실질적인 소스가 이미 있는
상황에서 상대적으로 작은 "장르 다양성용" 묶음을 정리한, 한국어 쪽(Wikipedia_설명문/
Wikisource_문학 제거)과 같은 방향의 결정. 그 결과 **docx 포맷 전체(한국어+영어)에서
문학/교재 장르가 사라지고 업무·행정·법률 문서 위주로 좁혀짐** - 서술형 산문·설명문은
이제 `대용량문서/`(한국어, 인간문제·민법·대한민국)가 전담한다. 영어 쪽에 짝이
되는 대용량 단일 문서(예: 챕터 분리 없는 장편소설 전문)를 넣을지는 보류 중.

## 알려진 이슈 (부분 해결됨)
`eval/timestamp_align.py: align_by_overlap`에 원래 신뢰도 임계값이 없었음 — 정렬이 틀려도
항상 최선의(하지만 틀린) 매치를 반환해서, 오정렬된 큐가 스코어링에서 제외되지 않고 엉뚱한
참조와 비교되어 채점됐음. About Time 25.6%, Interstellar 37.4%가 Jaccard 0.7 미만이었는데,
**직접 원문 검증(Tiedemann 2008 "Synchronizing Translated Movie Subtitles" 논문, pypdf로
원문 직접 확인)한 결과 OPUS 정렬 방식이 "1:0 정렬"(대응 없음)을 명시적으로 허용한다는 걸
확인** — 우리 구현엔 이게 빠져있었음. **수정 완료**: 최선 매치조차 실제 시간 겹침이 0이면
(한쪽 자막에만 그 구간이 존재 - 삭제/누락된 장면) 빈 문자열을 반환하도록 변경, 기존
`compare_per_cue()`의 `if p.strip() and r.strip()` 필터가 자동으로 스코어링에서 제외함(다른
파일 변경 불필요). 실측 배제 개수: About Time 35개, Interstellar 33개, Truman 0개(전체 대비
1.7~3%) — 원문 대조로 확인된 진짜 누락(예: Interstellar "Eject." 구간, 한국어 자막에서
127초간 통째로 빠짐)만 정확히 잡아냄.

**중요 발견(48샘플 수동 대조, About Time en2ko)**: 위 수정 후 남은 낮은 점수(Jaccard<0.7)
큐들의 **95~96%는 실제로 내용이 맞게 정렬돼있음** — 짧은 큐의 타이밍 민감성 + 참조 자막이
직역이 아니라 의역/현지화된 것이라 표면 글자 일치가 낮게 나올 뿐. 즉 **낮은 chrF/BLEU 평균의
근본 원인은 정렬 오류가 아니라 평가 방법론 자체의 구조적 천장**(OPUS식 자막-쌍 참조는 직역이
아님 + 한국어 표현의 다양성 + 모델 자체 번역 품질)이라는 게 확인됨 — RFP의 "한계의 솔직한
서술" 항목에 이 발견을 포함할 것. (아직 안 한 것: 간격이 1초 이상이지만 0은 아닌 애매한
케이스에 대한 처리 — 시도는 해봤지만 지표 개선 효과가 미미해서(추정 +0.3 chrF) 보류함.)

## 시도했지만 효과 없었던 것 (재시도 방지용 기록)
- 동적 few-shot 예시 선택(bge-m3 유사도 top-3) — chrF -1.09, 오히려 악화(타 영화 풀 톤 불일치).
- 영화 메타정보(제목/줄거리) 프롬프트 주입 — +0.18~0.19 chrF, 미미한 효과.
- 장르/카테고리 태그(위키피디아 카테고리 등) 프롬프트 주입 — 직접 시도는 안 했지만, 가장 가까운
  선행연구(EN-일본어 비즈니스 대화 문맥인식 NMT, arXiv:2311.11976, 원문 직접 확인)에서 장르/상황
  태그만 추가했을 때 BLEU +0.15로 우리 위 실험과 같은 수준의 미미한 효과 - 도입 안 함.
- 격식체/존댓말 제어(prompt로 formality 지정) — 우리 세팅(로컬 사전학습 instruct 모델 + 순정
  transformers.generate())에 바로 옮겨올 수 있는 검증된 메커니즘을 문헌에서 못 찾음(찾은 유일한
  formality-control 논문은 힌디어 대상 마스크드 토큰 분류 방식이라 전이 불가). 문헌상 실제 효과가
  있었던 건 정적 태그가 아니라 화자 간 관계/대화 맥락(문서 수준)이었는데 그마저 +0.37 BLEU 정도로
  작고, 구현 난이도(문서 수준 화자-맥락 모델링)가 이득 대비 커서 보류.
- vLLM 마이그레이션 — 아키텍처 지원 자체는 있지만(`Gemma4UnifiedForConditionalGeneration`),
  **이 서버의 NVIDIA 드라이버(510.108.03, CUDA 11.6)가 vLLM nightly 빌드(cu129)보다 너무
  오래돼서 import 단계에서 실패**(`undefined symbol: cuTensorMapEncodeTiled`). 구버전 vLLM
  빌드를 시도하지 않는 한 이 환경에선 막혀있음. 격리 venv에 설치했다가 실패 확인 후 완전
  삭제함(오늘 새로 받은 uv 캐시도 정리, 8/29 생성된 기존 캐시는 보존).

## 환경 주의사항
- **GPU(RTX 3090, 24GB)는 물리적으로 다른 워크로드와 공유됨** — 컨테이너 자체는 전용
  (`PID 1 = jupyter-lab`, 로그인 사용자 root 하나뿐)으로 보이지만, `nvidia-smi`에 우리
  프로세스 목록(`ps`)에 없는 다른 PID가 수시로 나타나 GPU 메모리를 크게 잡았다 놓았다 함
  (실측: 한 번은 이것 때문에 우리 배치 테스트가 실제 OOM 남). 배치 크기나 새 라이브러리
  로드 전엔 `nvidia-smi`로 여유를 확인하는 습관이 필요.
- 배치 크기별 GPU 메모리 실측(batch=1~4, 8청크 기준): 1→9.98GB, 2→10.29GB, 3→10.76GB,
  4→11.01GB peak allocated — 배치를 4배로 늘려도 메모리는 ~10%만 증가(효율적).

## 향후 계획 (미착수)
- threshold + min_chunk_tokens 합동 튜닝.
- `align_by_overlap` 신뢰도 기반 제외 로직 추가 여부 결정.
- Late chunking(arXiv:2409.04701)은 검토 후 배제 결정 — 일반적인 semantic chunking 관행이
  아니고(임베딩 기법이지 경계선택 기법이 아님), bge-m3 컨텍스트 한도(8192토큰, 직접 확인)가
  자막 전체 문서(About Time 16,072토큰, 직접 측정)엔 부족해서 추가 확장 로직이 필요함.
